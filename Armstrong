#checks if a number is armstrong or not
#Armstrong=> where the sum of digits raised number of digits gives the same number
import math
a = int(input())
A = str(a)
p = len(A)
sum = 0
i = 0
while(i<p):
    sum+=math.pow(int(A[i]),p)
    i+=1
if(sum==a):
    print("True")
else:
    print("False")
